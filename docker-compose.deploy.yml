# 部署版 — 用于服务器生产环境
# 镜像从 Docker Hub 拉取（需设置 DOCKER_USERNAME 环境变量）
services:
  frontend:
    image: ${DOCKER_USERNAME}/virtual-pal-frontend:latest
    container_name: virtual-pal-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    image: ${DOCKER_USERNAME}/virtual-pal-backend:latest
    container_name: virtual-pal-backend
    ports:
      - "8000:8000"
    environment:
      - REDIS_HOST=redis
    depends_on:
      redis:
        condition: service_started
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: virtual-pal-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
